export enum PLAYBACK_STATE {
    PLAYING = 'playing',
    PAUSED = 'paused',
    STOPPED = 'stopped',
}

export enum REPEAT_MODE {
    REPEAT_OFF = "repeat-off",
    REPEAT_ALL = "repeat-all",
    REPEAT_SINGLE = "repeat-single",
}

export enum SHUFFLE_MODE {
    SHUFFLE_OFF = "shuffle-off",
    SHUFFLE_ON = "shuffle-on",
}